{
  "name": "companies-data-demo",
  "version": "1.0.0",
  "description": "the data of public companies from dynamo db",
  "main": "app.js",
  "repository": "https://github.com/AHaydar/companies-data-demo",
  "author": "Ali Haydar",
  "license": "MIT",
  "scripts": {
    "build": "sam build --manifest ./package.json",
    "deploy": "sam build --manifest ./package.json && sam deploy --capabilities CAPABILITY_NAMED_IAM"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/util-dynamodb": "^3.1.0",
    "axios": "^0.21.1"
  },
  "devDependencies": {
    "jest": "^27.0.6"
  }
}
